name: My First CI Pipeline

on:
  push:
    branches:
      - dev   # since you are on dev branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Run Python Code
        run: python app.py